import React from 'react';

interface ScreenConfigurationProps {}

function ScreenConfiguration(_: ScreenConfigurationProps) {
  return <div>sdfsdf</div>;
}

export default React.memo(ScreenConfiguration);
